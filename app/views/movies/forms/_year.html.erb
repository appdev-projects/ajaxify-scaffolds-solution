<div id="<%= dom_id(movie) %>_year_form">
  <%= form_with(model: movie, local: false) do |form| %>
    <div class="field">
      <%= form.label :year %>
      <%= form.text_field :year %>
    </div>
  <% end %>
</div>

